# Ternary

# "Condition_if_true" if Condition_if_true else "condition_if_false"

# lets suppose facebook message access
is_friend = False

can_message = "Messages Allowed" if is_friend else "Messages Not Allowed"

print(can_message)